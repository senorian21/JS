const areaOrPerimeter = function(l , w) {
  if (l === w){
    return l * w
  } else {
    return (l + w) * 2
  }
};

// const areaOrPerimeter = function(l , w) {
//   return l === w ? l * w : (l + w) * 2
// };